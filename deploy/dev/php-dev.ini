; Development-only stability override. Windows bind mounts make timestamp
; validation across Laravel's dependency tree slow enough to stall browser
; requests. Restart only the app container after PHP changes instead.
opcache.validate_timestamps = 0
opcache.revalidate_freq = 0
