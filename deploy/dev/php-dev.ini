; Development-only OPcache override. The production image keeps timestamp
; validation disabled, while bind-mounted source must be revalidated in dev.
opcache.validate_timestamps = 1
opcache.revalidate_freq = 0
