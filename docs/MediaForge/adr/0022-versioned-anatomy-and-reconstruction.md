# ADR-0022 — Versionierte Anatomie und immutable Reconstruction Revisions

Körperregionen verwenden stabile hierarchische IDs. 3D-Rekonstruktionen werden versioniert statt in-place überschrieben; Coverage bleibt mit Model-/Ontology-Version reproduzierbar.
